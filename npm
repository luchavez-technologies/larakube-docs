#!/bin/bash

# LaraKube NPM Wrapper (Daemon-Aware)
# Forwards all arguments to the persistent Node daemon

# Ensure we are in the docs directory
cd "$(dirname "$0")"

# Execute npm via the node daemon
./node npm "$@"
